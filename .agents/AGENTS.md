# Style Guidelines & Behavioral Rules

- Never draft, tag, or create a GitHub release/git tag unless specifically and explicitly instructed by the user.
- When committing code to git, push to a feature branch and create a Pull Request (PR) for merging into the main branch instead of pushing directly to main.
